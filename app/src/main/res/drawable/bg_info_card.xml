<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color references theme color -->
    <solid android:color="@color/info_card_bg"/>
    <!-- Border color references theme color -->
    <stroke
        android:width="1dp"
        android:color="@color/info_card_border"/>
    <corners android:radius="24dp"/>
</shape>
