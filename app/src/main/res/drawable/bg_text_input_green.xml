<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- White background -->
    <solid android:color="@color/colorWhite" />

    <!-- Rounded corners -->
    <corners android:radius="20dp" />

    <!-- Gray border -->
    <stroke
        android:width="2dp"
        android:color="@color/txtGreen" />
</shape>
