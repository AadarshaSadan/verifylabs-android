<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    android:background="@color/app_background_before_login"
    tools:context=".presentation.auth.signup.SignUpActivity">

    <!-- Action Bar with Back Button (Fixed, Non-Scrollable) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:orientation="horizontal"
        android:gravity="start|center_vertical">

        <LinearLayout
            android:id="@+id/btnBack"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:layout_gravity="center_vertical"
            android:background="?android:selectableItemBackgroundBorderless"
            android:padding="8dp">

            <ImageButton
                android:id="@+id/imgBack"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_back"
                android:layout_gravity="center_vertical"
                android:background="@null"
                android:contentDescription="@string/back_button" />

            <TextView
                android:id="@+id/tvBack"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/back"
                android:textStyle="bold"
                android:layout_gravity="center_vertical"
                android:textColor="@color/colorBlack"
                android:textSize="16sp"
                android:layout_marginStart="8dp" />
        </LinearLayout>
    </LinearLayout>

    <!-- Scrollable Content -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:overScrollMode="always"
        android:scrollbars="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp"
            android:gravity="center_horizontal">

            <!-- App Logo -->
            <androidx.cardview.widget.CardView
                android:id="@+id/imgAppIcon"
                android:layout_width="120dp"
                android:layout_height="40dp"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="16dp"
                android:contentDescription="@string/app_icon"
                android:elevation="2dp">

                <ImageView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:src="@drawable/verifylabs_logo"
                    android:scaleType="fitXY" />
            </androidx.cardview.widget.CardView>

            <!-- Header Text -->
            <TextView
                android:id="@+id/tvWelcome"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/create_account"
                android:textSize="24sp"
                android:textColor="@color/colorBlack"
                android:textAppearance="@style/TextAppearance.AppCompat.Large"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvSignUpPrompt"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:textSize="14sp"
                android:paddingHorizontal="16dp"
                android:text="@string/join_verifylabs"
                android:textColor="@color/verifylabs_dots_indicator"
                android:textAppearance="@style/TextAppearance.AppCompat.Medium" />

            <!-- Full Name -->
            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etUsername"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="18dp"
                android:inputType="textPersonName"
                android:padding="12dp"
                android:textSize="16sp"
                android:hint="@string/enter_full_name"
                android:drawablePadding="12dp"
                android:paddingHorizontal="15dp"
                android:drawableStart="@drawable/ic_user"
                android:background="@drawable/bg_text_input"
                android:textColor="@color/colorBlack"
                android:contentDescription="@string/full_name_input" />

            <!-- Email -->
            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etEmail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="18dp"
                android:inputType="textEmailAddress"
                android:padding="12dp"
                android:textSize="16sp"
                android:hint="@string/enter_email"
                android:drawablePadding="12dp"
                android:paddingHorizontal="15dp"
                android:drawableStart="@drawable/ic_email"
                android:background="@drawable/bg_text_input"
                android:textColor="@color/colorBlack"
                android:contentDescription="@string/email_input" />

            <!-- Username -->
            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etFullUsername"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="18dp"
                android:inputType="text"
                android:padding="12dp"
                android:textSize="16sp"
                android:hint="@string/choose_username"
                android:drawablePadding="12dp"
                android:paddingHorizontal="15dp"
                android:drawableStart="@drawable/ic_user_profile"
                android:background="@drawable/bg_text_input"
                android:textColor="@color/colorBlack"
                android:contentDescription="@string/username_input" />

            <!-- Password -->
            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etPassword"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="18dp"
                android:inputType="textPassword"
                android:padding="12dp"
                android:textSize="16sp"
                android:hint="@string/enter_password"
                android:drawablePadding="12dp"
                android:paddingHorizontal="15dp"
                android:drawableStart="@drawable/ic_password_signup"
                android:background="@drawable/bg_text_input"
                android:textColor="@color/colorBlack"
                android:contentDescription="@string/password_input" />

            <!-- Confirm Password -->
            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etConfirmPassword"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="18dp"
                android:inputType="textPassword"
                android:padding="12dp"
                android:textSize="16sp"
                android:hint="@string/confirm_your_password"
                android:drawablePadding="12dp"
                android:paddingHorizontal="15dp"
                android:drawableStart="@drawable/ic_password_signup"
                android:background="@drawable/bg_text_input"
                android:textColor="@color/colorBlack"
                android:contentDescription="@string/confirm_password_input" />

            <!-- Create Account Button -->
            <LinearLayout
                android:id="@+id/btnCreateAccount"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:orientation="horizontal"
                android:gravity="center"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:background="@drawable/drawable_verify_background_btn_failed_likely_gray"
                android:clickable="true"
                android:focusable="true"
                android:layout_marginTop="30dp"
                android:contentDescription="@string/create_account_button">

                <TextView
                    android:id="@+id/tvCreateAccount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/create_account"
                    android:textColor="@android:color/white"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:layout_marginStart="8dp"
                    android:layout_gravity="center_vertical" />
            </LinearLayout>

            <!-- Sign In Prompt -->
            <LinearLayout
                android:id="@+id/btnSignInPrompt"
                android:background="?android:selectableItemBackground"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:padding="20dp"
                android:layout_marginTop="1dp"
                android:layout_marginBottom="16dp"
                android:gravity="center">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/already_have_account"
                    android:textSize="14sp"
                    android:textColor="@color/colorBlack" />

                <TextView
                    android:layout_marginStart="5dp"
                    android:id="@+id/tvSignIn"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/sign_in"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="@color/txtGreen"
                    android:clickable="true"
                    android:focusable="true" />
            </LinearLayout>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</LinearLayout>